services:
  - type: web
    name: polymarket-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --workers 1 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
